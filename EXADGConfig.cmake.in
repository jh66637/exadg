SET(EXADG_VERSION @VERSION@)

@PACKAGE_INIT@

GET_FILENAME_COMPONENT(EXADG_CMAKE_DIR "${CMAKE_CURRENT_LIST_FILE}" PATH)

SET(EXADG_GIT_BRANCH "@EXADG_GIT_BRANCH@")
SET(EXADG_GIT_SHORTREV "@EXADG_GIT_SHORTREV@")

SET(EXADG_INCLUDE_DIRS "@PACKAGE_EXADG_INCLUDE_DIRS@")

INCLUDE("${CMAKE_CURRENT_LIST_DIR}/EXADGTargets.cmake")
INCLUDE("${CMAKE_CURRENT_LIST_DIR}/EXADGOptions.cmake")

FILE(GLOB macro_files "@PACKAGE_EXADG_MACRO_DIR@/*.cmake")
FOREACH(file ${macro_files})
  INCLUDE(${file})
ENDFOREACH()
